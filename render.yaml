services:
  - type: web
    name: zing-payments-redesign
    runtime: static
    plan: free
    buildCommand: echo "No build step required"
    staticPublishPath: .
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
